import useQuery from '@app/hooks/use-query';
import { FC } from 'react';

const Room: FC = () => <div></div>;

const Rooms: FC = () => {
  // const { response, error, loading } = useQuery({
  //   fetchFn: () => axiosInstance.get<ManualAuthResponse>('/auth/sign-in')
  // });

  return <div>Games</div>;
};

export default Rooms;
